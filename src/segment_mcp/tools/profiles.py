"""`lookup_profile` — Profile API `/traits`, `/external_ids`, `/events`, `/links`.

Separate trust tier from every other tool in this package — see
`client/profile_api.py` and BUILD-PLAN.md §5 (v0.2). Implemented in Prompt 3.
"""
